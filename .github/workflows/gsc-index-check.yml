name: GSC Index Check

on:
  schedule:
    - cron: '0 10 * * 1,3,5'  # Mon/Wed/Fri 10:00 UTC
  workflow_dispatch:

permissions:
  contents: read

jobs:
  gsc-index-check:
    uses: cybrixcc/leadhunter-engine/.github/workflows/gsc-index-check.yml@master
    with:
      config_path: ./config.yml
    secrets: inherit
