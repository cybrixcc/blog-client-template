name: GEO Health Check

on:
  schedule:
    - cron: '0 9 * * 1'  # Mondays 09:00 UTC
  workflow_dispatch:

jobs:
  geo-health-check:
    uses: cybrixcc/leadhunter-engine/.github/workflows/geo-health-check.yml@main
    with:
      config_path: ./config.yml
      blog_glob: 'src/app/blog/*/page.tsx'  # adjust to your blog file structure
    secrets: inherit
