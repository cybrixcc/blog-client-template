name: AI Citation Research

on:
  schedule:
    - cron: '0 10 * * 3'  # Wednesdays 10:00 UTC
  workflow_dispatch:

permissions:
  contents: read
  issues: write

jobs:
  citation-research:
    uses: cybrixcc/leadhunter-engine/.github/workflows/ai-citation-research.yml@master
    with:
      config_path: ./config.yml
    secrets: inherit
